// Calculate the sum of two numbers, and return the result

const num1 = 10;
const num2 = 20;

const sum = num1 + num2;

console.log(`The sum of ${num1} and ${num2} is ${sum}`);


// Javascript function to square the numbers
function squareNumbers(number) {
    return number * number; 
  }
  
  for (let i = 1; i <= 10; i++) {
    const squaredValue = squareNumbers(i);
    console.log(`The square of ${i} is ${squaredValue}`);
  }